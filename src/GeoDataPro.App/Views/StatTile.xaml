<UserControl x:Class="GeoDataPro.App.Views.StatTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Border Style="{StaticResource Card}" Padding="16" Margin="0,0,12,0" MinWidth="152">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Caption, ElementName=Root}" Style="{StaticResource Caption}" />
            <Border Grid.Column="1" Width="24" Height="24" Background="{StaticResource AccentSoft}" CornerRadius="12">
                <Ellipse Width="8" Height="8" Fill="{StaticResource Accent}" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,8,0,0">
                <TextBlock Text="{Binding Value, ElementName=Root}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" />
                <TextBlock Text="{Binding Unit, ElementName=Root}" FontSize="12" Foreground="{StaticResource TextMuted}"
                           VerticalAlignment="Bottom" Margin="6,0,0,4" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
